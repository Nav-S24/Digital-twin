"""
Vehicle Knowledge Base - RAG application package.

Exposes the package version and marks `app/` as an importable Python
package so `from app.config import settings`-style imports resolve
correctly regardless of the current working directory.
"""

__version__ = "2.0.0"
